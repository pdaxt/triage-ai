{
  "name": "triage-ai-demo",
  "type": "module",
  "scripts": {
    "audio": "bash generate-audio.sh",
    "record": "node record-playwright.js",
    "combine": "ffmpeg -i recordings/demo-raw.webm -i audio/voiceover.mp3 -c:v libx264 -c:a aac -shortest recordings/triageai-demo.mp4"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  }
}
